# MOGU 2.4 Acceptance Report

```yaml
date: 2026-07-24
branch: capability/2.4
baseline: capability/2.3@8e402a6
acceptance:v2.4: PASS 34/34
audit:v2.4-security: PASS
npm test: PASS 371/371
promotion_recommendation: KEEP_DEFAULT_OFF
```

## Summary

Control Plane MVP landed: Registry / Brain Manager / Discovery / Dependency Supervisor / Remote Center / First Run Wizard + minimal settings Hub UI. No Runtime rewrite, no new Agent, no new npm runtime dependencies.

## Results

| Gate | Result |
|------|--------|
| `npm run acceptance:v2.4` | **34/34 PASS** |
| `npm run audit:v2.4-security` | **PASS** |
| `npm test` | **371/371 PASS** |

Evidence: `benchmarks/v2.4/results/acceptance.json`, `benchmarks/v2.4/results/security-audit.json`

## Covered acceptance intents

1. First-run wizard status / choose local  
2. Select Ollama model (hot apply)  
3. Chat path readiness via Brain set  
4–5. Switch OpenAI provider immediately (no restart)  
6. Telegram token → SecretStore + Remote Center status  
7–8. Missing ComfyUI dependency hint before failure  
9. SecretStore-only remote tokens  
10. Default-Off (`controlPlaneEnabled` etc.)

## Incomplete / follow-ups (not blockers for Default-Off land)

- Auto-start / auto-install actions still **prompt-only** unless flags enabled (by design)  
- QQ / WeChat Remote Center is status-only (adapters remain transport stubs)  
- First-run wizard UI is thin (Control Plane tab + choose buttons); full marketing wizard polish deferred  
- Live Electron field soak of Control Plane tab not required for this gate  
- GPT-5.6 exact model lock still owner-gated (2.1 holdout unchanged)

## Default-On?

**No.** Recommend **KEEP_DEFAULT_OFF**. Enable via wizard complete or explicit「开启控制中心」.
