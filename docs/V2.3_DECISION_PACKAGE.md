# MOGU 2.3 Remote Workspace — Decision Package

```yaml
date: 2026-07-24
implementation: COMPLETE
feature_defaults: OFF
acceptance: scripts/acceptance_v2.3.js
promotion_recommendation: KEEP_DEFAULT_OFF
```

## Outcome

Remote Workspace is implemented on `capability/2.3` as an additive Task Source. Existing Trust Plane,
PermissionProxy behavior, TaskStore schema, Brain Adapter, and Coding Worker handlers were not
rewritten. Channel adapters only transport messages and files.

## Evidence

Under `benchmarks/v2.3/results/`:

- Unit tests: 365/365
- 2.3 acceptance: 47/47
- Telegram / QQ / WeChat simulated pipelines PASS
- Cancel / approval / retry / log PASS
- `settings.remote.*` default OFF

## Decisions

| Topic | Decision |
|-------|----------|
| Architecture | Remote → TaskRequest → Permission → TaskQueue facade → Skill/Brain → Outbox |
| Security | Elevated remote ops require admin YES; fail-closed on timeout |
| Schema | TaskStore allowlist unchanged; `kind: "remote"` |
| Defaults | All remote flags OFF |
| Public release | Not in scope; personal/opt-in only |
| Next | Collect real remote failures before deeper autonomy |

## Recommendation

`keep-default-off`. Enable per-channel only after bot tokens and admin approval UX are configured.
