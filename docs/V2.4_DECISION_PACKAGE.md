# MOGU 2.4 Capability Center — Decision Package

```yaml
date: 2026-07-24
codename: MOGU Control Plane
branch: capability/2.4
baseline: capability/2.3@8e402a6
status: IMPLEMENTATION_LANDED
implementation: PHASE_1_TO_7_MVP
feature_defaults: OFF
promotion_recommendation: KEEP_DEFAULT_OFF
north_star: >
  让用户不需要懂 MOGU 内部器官，但能控制全部器官。
```

## Positioning

```text
2.4 ≠ Capability Center 页面堆砌
2.4 ＝ MOGU Control Plane 收口
```

## Delivered modules

```text
src/main/control-plane/
  CapabilityRegistry.js      # 只读聚合现有 Adapter / Preflight / SecretStore
  CapabilityDiscovery.js     # READY / NOT_READY + 用户可读修复建议
  BrainManager.js            # 热切换 Brain，密钥仅 SecretStore
  DependencySupervisor.js    # 任务前依赖提示（Default-Off 自动安装）
  RemoteCenter.js            # Telegram/QQ/微信状态；Token→SecretStore
  FirstRunWizard.js          # 意图优先首次流程
  index.js                   # createControlPlane facade
```

IPC：`control-plane:*`（见 preload）  
UI：设置 Hub「控制中心」页（最少必要，非新 Dashboard）

## Default-Off

```text
controlPlaneEnabled: false
autoStartServices: false
autoInstallRuntime: false
modelFallback: false
backgroundSupervisor: false
remote.*: false（继承 2.3）
```

## Non-goals（仍冻结）

多 Agent / DAG / 自愈 / 自动模型路由 / 新 Skill / 重写 Runtime / 新数据库 / 遥测 / 替换 OpenClaw

## Acceptance

- `npm run acceptance:v2.4`
- `npm run audit:v2.4-security`
- `npm test`（含 `tests/v24-control-plane.test.js`）

报告：`docs/V2.4_ACCEPTANCE_REPORT.md`

## Charter lock

> Only allow work that makes users control every MOGU organ without needing to understand those organs.
