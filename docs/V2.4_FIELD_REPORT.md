# MOGU 2.4 Field Report — Control Plane 产品化收口

```yaml
date: 2026-07-24
branch: capability/2.4
commit: 1c8da99 (+ field probe scripts / this report)
worktree: D:\Project\ai-model-manager-2.4
recommendation: KEEP_DEFAULT_OFF
field_probe: benchmarks/v2.4/results/field-probe.json (24/24 PASS after fix)
created_2.5: false
```

## 1. 测试环境

| 项 | 值 |
|----|-----|
| OS | Windows 10 (build 26200) |
| App | Electron `npm start` from `ai-model-manager-2.4` |
| userData | `%APPDATA%\ai-model-manager`（未改 appId） |
| Brain | OpenAI-compatible → Ollama `http://127.0.0.1:11434/v1` / `qwen2.5-coder:7b` |
| Control Plane | 场测临时 `controlPlaneEnabled=true`（**代码默认仍 false**） |
| Remote | Telegram enabled + owner 已绑定；Token 仅 SecretStore |
| Ollama | 本机已装；含 `qwen2.5-coder:7b` 等 |
| PAI / ComfyUI | 本机场测时 **PAI 未运行**（见问题） |

## 2. 证据（非 UI 截图）

本轮未嵌入 Electron 窗口截图；以可复现探针为准：

| 证据 | 路径 / 结果 |
|------|-------------|
| Control Plane 场测探针 | `benchmarks/v2.4/results/field-probe.json` → **24/24 PASS** |
| 安全审计（工程门） | `benchmarks/v2.4/results/security-audit.json` → PASS |
| Acceptance | `acceptance:v2.4` 34/34（工程阶段） |
| Telegram 场测通知 | `scripts/notify_field_status.js` → `notified: true`（手机应收到 Field Status） |
| Ollama 实聊 | `chat/completions` 返回正常（模型在线） |
| App 日志 | `remote workspace start … channels:["telegram"]` |

复现探针：

```text
npx electron scripts/field_probe_v24.js
npx electron scripts/notify_field_status.js
```

## 3. Phase 结果

### Phase 1 — 桌面真实场测

| 检查 | 结果 |
|------|------|
| 开启 `controlPlaneEnabled=true`（仅本机 settings） | ✅ |
| 发现本地 `qwen2.5-coder:7b` | ✅ |
| 云端 OpenAI-compatible 可配置（本场测用本机 Ollama endpoint） | ✅ |
| Brain 切换 local ↔ api **立即生效**、`restartRequired=false` | ✅ |
| Discovery checklist（Ollama/OpenClaw/PAI/Comfy/FFmpeg/Docker…） | ✅ |
| 缺失项给出状态/原因，不直接进程崩溃 | ✅ |
| 公开快照无端口 / 无明文 Key / 无 `E:\Project` | ✅ |

### Phase 2 — First Run 路径

| 步骤 | 结果 |
|------|------|
| 代码默认未配置时 Control Plane DISABLED | ✅（`DEFAULT_SETTINGS.controlPlaneEnabled=false`） |
| 打开控制中心 → 选择 Brain → SecretStore → 检测 | ✅ 工程路径可用（设置 Hub「控制中心」） |
| 用户无需理解 localhost/port/Adapter/IPC | ⚠️ **部分达标**：公开 Discovery/Status 已隐藏端口与路径；高级设置页仍可能出现技术字段 |
| 完整「5 分钟空白机营销向导」体验 | ⚠️ **薄实现**：有 Wizard API + 控制中心按钮，非独立完整 Onboarding 叙事页 |

### Phase 3 — Remote

| 检查 | 结果 |
|------|------|
| Remote 自启 Telegram | ✅ 日志 `channels:["telegram"]` |
| 向 Owner 推送 Field Status（Brain/Model/ControlPlane） | ✅ |
| 手机 `/status` /「分析我的项目」真链路 | ⏳ **需 Owner 手机确认**（已发引导通知） |
| Tool 闭环 | ⚠️ 历史场测：`mogu_pc.search` 因 **PAI 未启动** 失败；Dependency Supervisor 可提示依赖，但本机器官未全绿 |

### Phase 4 — 安全

| 检查 | 结果 |
|------|------|
| `settings.json` 中 `agentApiKey` 为空 | ✅ |
| Telegram Token 不在 settings，仅 secrets 密文引用 | ✅ |
| 探针/通知脚本不打印密钥 | ✅ |
| 未写入开发机 `E:\Project` 到 `paiRoot` | ✅（当前为空） |
| 新用户空环境 | ⚠️ 本机 userData **已有** Remote/Token/模型配置，**不是**全新安装机；空白机 EXE 路径仍需另测 |

### Phase 5 — Default-On 决策

闸门要求：连续 7 天稳定 + 本地/云端切换 + Remote 稳定 + First Run 无阻塞。

| 闸门 | 本轮 |
|------|------|
| 7 天真实运行 | ❌ 未满足 |
| 本地/云端切换正常 | ✅ 探针通过 |
| Remote 稳定 | ⚠️ 连通，但工具依赖（PAI）未全绿；手机真任务待确认 |
| First Run 无阻塞 | ⚠️ 薄向导，空白机未测 |

**结论：KEEP_DEFAULT_OFF**  
代码默认保持 `controlPlaneEnabled=false`。本机场测可临时开启，**不得**因此把默认改成 true。

## 4. 问题与处理

| 问题 | 严重度 | 处理 / 状态 |
|------|--------|-------------|
| 场测探针初版 `listModels` 因 `OllamaService` 解构错误失败 | 低 | 已修 `scripts/field_probe_v24.js`，复跑 24/24 |
| PAI 未运行 → `mogu_pc` 搜索失败（日志含端口提示） | 中 | Control Plane 应对用户隐藏端口；产品侧依赖提示走 Supervisor，不改 Runtime。场测机需启动 PAI 或跳过 PC 技能 |
| First Run UI 过薄 | 中 | 不堆功能；记录为产品化后续 polish，不进 2.5 |
| 空白机未测 | 中 | 保持 Default-Off，待干净 profile / 新 userData 再验 |
| Electron 进程 Path 显示 2.3 `node_modules` | 低 | 2.4 worktree junction 复用依赖；应用 cwd 为 2.4 |

## 5. 修复（本轮仅场测收口，无产品功能）

- 新增 `scripts/field_probe_v24.js`（可复现桌面探针）
- 新增 `scripts/notify_field_status.js`（Telegram 场测通知）
- 本报告 `docs/V2.4_FIELD_REPORT.md`
- **未**改 Default-On
- **未**创建 2.5
- **未**新增 Skill / Agent / DAG / 依赖

## 6. 是否建议 Default-On？

### **否 — KEEP_DEFAULT_OFF**

理由：7 天稳定性闸门未过；First Run / 空白机未闭环；Remote 工具依赖（PAI）在本机未 READY。

建议 Owner 继续：

1. 手机确认 `/status` 与「分析我的项目」  
2. 需要 PC 技能时启动 PAI，再看 Supervisor 提示是否友好  
3. 另开干净 `MOGU_USER_DATA` 跑 First Run  
4. 连续使用满 7 天后再复议 Default-On

## 7. 产品判断

架构层（Brain / Tool / Trust / Evidence / Remote / Control Plane）已齐。  
当前缺口是 **产品化体验与场测时长**，不是继续堆能力。

目标形态：

> 打开电脑 → 点 MOGU → 选模型 → 直接干活

本轮验证：工程开关 + Brain 热切换 + Discovery/安全边界 **可用**；完整普通用户路径仍需空白机与多日浸泡。
