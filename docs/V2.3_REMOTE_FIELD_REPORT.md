# MOGU 2.3 Remote Field Report (7-day)

```yaml
status: TEMPLATE · fill during personal soak
branch: capability/2.3
channel: telegram-only
qq_wechat: OFF
```

## Owner setup checklist

- [ ] BotFather `/newbot` → BOT_TOKEN
- [ ] Token stored via `remote:set-telegram-token` / SecretStore (`telegramBotToken`) — **not** settings.json / .env
- [ ] `remoteOwner.telegramUserId` bound to your numeric Telegram user id
- [ ] Settings:

```json
{
  "remote": {
    "enabled": true,
    "telegram": { "enabled": true },
    "qq": { "enabled": false },
    "wechat": { "enabled": false },
    "requireApproval": true,
    "allowAutoExecute": false
  },
  "remoteOwner": {
    "telegramUserId": "YOUR_ID"
  }
}
```

- [ ] Desktop MOGU running; `remote:start` ok
- [ ] Phone: `/status` returns workstation block

## Daily log

| Day | Tests | Result | Notes |
|-----|-------|--------|-------|
| 1 | `/status` stability | | |
| 2 | create task + TaskStore/Trace | | |
| 3 | long task + lock screen + progress | | |
| 4 | network drop / resume notify | | |
| 5 | dangerous op without YES = blocked | | |
| 6 | 20-task stress, retry, cancel | | |
| 7 | summary metrics | | |

## Target metrics (Day 7)

| Metric | Target | Observed |
|--------|--------|----------|
| Message success | >99% | |
| Task completion | >90% | |
| Mis-execution | 0 | |
| Permission bypass | 0 | |
| Lost notifications | 0 | |

## Incidents

| When | Symptom | Root cause | Fix |
|------|---------|------------|-----|
| | | | |

## Decision for 2.4

- [ ] Not yet — continue soak / fix issues
- [ ] Ready to discuss Self-Healing inputs only after metrics met

**Frozen while soaking:** 2.4, DAG, multi-agent, memory evolution, auto-learning.
