# mogu.studio

## 何时使用
- 用户在创作台参数化出片（人物/动作/工作流/尺寸）
- 需要预检后出片，或同参数失败重试

## 操作
- `preflight` — Comfy 离线 / 缺工作流 / 参考图路径
- `run` — 参数化出片；写入 provenance（工作流、参数、耗时、taskId）
- `retry` — 使用上一任务 `replay` 同参再跑

## 权限
- L2（`mogu.studio`）

## 禁止
- 预检失败时不要强行提交
- 不要自动打开外部剪辑工具（长片去视频合成）
